%h1
  Projects
  = link_to 'Add New', new_project_path, class: 'btn btn-success'

%table.table.table-condensed.table-bordered.table-striped.table-hover
  %tr
    %th
    %th Name

  - @projects.each do |project|
    %tr
      %td
        = link_to icon('search'), project
        = link_to icon('pencil'), edit_project_path(project)
        = link_to icon('trash'), project, :method => :delete, :data => { :confirm => 'Are you sure?' }
      %td= project.name